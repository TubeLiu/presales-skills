---
description: 内容正确性审查——一个方案章节撰写完成后，验证内容是否正确完整地满足计划要求（章节通过此审查后再走 quality-review）。
---

用户运行了 `/solution-master:spec-review $ARGUMENTS` —— 请激活 solution-master plugin 的 `spec-reviewing` skill 处理：

`$ARGUMENTS`

详细工作流见 `skills/spec-reviewing/SKILL.md`（前置：章节草稿已产出；后置：通过则交 quality-review）。
