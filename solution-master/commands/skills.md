---
description: skill 维护工具——为 Solution Master 框架创建新技能、编辑现有技能或在部署前验证技能是否有效。
---

用户运行了 `/solution-master:skills $ARGUMENTS` —— 请激活 solution-master plugin 的 `writing-skills` skill 处理：

`$ARGUMENTS`

详细工作流见 `skills/writing-skills/SKILL.md`（meta 工具：写 / 改 / 验证其他 skill）。
