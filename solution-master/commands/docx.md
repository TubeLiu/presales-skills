---
description: 输出标准 DOCX——把方案内容（Markdown）转成统一字体规范的 DOCX 文件。
---

用户运行了 `/solution-master:docx $ARGUMENTS` —— 请激活 solution-master plugin 的 `docx-formatting` skill 处理：

`$ARGUMENTS`

详细工作流见 `skills/docx-formatting/SKILL.md`（Markdown → DOCX，遵循统一字体规范）。
