.DS_Store
__pycache__/
*.pyc
*.pyo
**/node_modules/
.venv/
venv/
.env
.env.local
*.egg-info/
.pytest_cache/

# /plugin-review 产物（临时 review 报告，不入库）
REVIEW_FINDINGS.md

# === 工作流产物（F-038，不入库） ===
solution-master/drafts/
solution-master/output/
tender-workflow/drafts/
tender-workflow/output/
ppt-master/projects/*/
!ppt-master/projects/README.md
ppt-master/exports/
**/svg_output/
**/svg_final/
ai-image/skills/gen/.deps-installed
ppt-master/skills/make/.deps-installed
ai-image/skills/gen/.deps-installing.lock
ppt-master/skills/make/.deps-installing.lock

# vercel-labs/skills CLI 副产物（用户在 cwd 运行 `npx skills add ./` 实测时产生）
skills-lock.json
.agents/
.cursor/
.codex/
.opencode/

# 用户本地 Claude Code settings（F-053）
# 注：.claude/ 是用户本地 settings；.claude-plugin/ 是 plugin manifest，必须 tracked。两者不同。
# 例外：.claude/commands/ 下是项目级 slash command（如 /plugin-review），值得 commit 给协作者用。
.claude/*
!.claude/commands/
